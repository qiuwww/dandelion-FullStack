@import '../../styles/mixin.styl';

.fa {
  {ct};
  display: inline-flex;
  color: i-color;
  font-size: 24px;

  &-large {
    font-size: 30px;
  }

  &-middle {
    font-size: 24px;
  }

  &-small {
    font-size: 16px;
  }
}
