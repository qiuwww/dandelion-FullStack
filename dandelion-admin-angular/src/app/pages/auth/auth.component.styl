@import '../../styles/mixin.styl';

item-height = 36px;

.auth {
  wh(100%, 100%);
  min-height: 600px;
  min-width: 800px;
  {ct};
  background-color: #888;
  background-image: url('/assets/images/auth/bg.jpg');
  background-size: 100% 100%;
}

.center {
  width: 400px;
  padding: 20px;
  border-radius: 5px;
  background #b2356c url('/assets/images/auth/plum-flower.jpg') left top no-repeat;
  background-size cover
}

form {
  h2 {
    {ct};
    height: item-height;
    margin-bottom: 10px;
  }
}

div.form-group {
  width: 100%;
  height: item-height + 4px;
  margin-bottom: 10px;
  {cv};
  {fr};
  border: 1px solid #ccc;
  border-radius: 3px;
  background-color: #f9ffb7
  label {
    width: 36px;
    height: item-height;
    {ct};
  }

  input {
    flex: 1;
    height: item-height;
    padding-left: 5px;
    border: none;
    outline: none;
    bgc(transparent);
  }
}
button.submit
  width: 100%;
  height:item-height;
  bgc(#4280df)
  border-radius: 3px;
  color: #fff;
  {ct}
  font-size: 14px;
  border: none;
  cursor: pointer;
  margin-bottom: 10px;
  &:disabled
    background-color: #eee;
    color: #aaa;
    cursor: auto;
.more
  width: 100%;
  height: 30px;
  {cv};
  justify-content: flex-end;
  a
    wh(112px, 100%)
    {ct}
.eye
  width: item-height;
  height: @width;
  {ct}
  {cp}
